[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# Put the backend dir (rootdir) on sys.path so `app` imports under the bare
# `pytest` console script used in CI, not just `python -m pytest` locally.
pythonpath = .
